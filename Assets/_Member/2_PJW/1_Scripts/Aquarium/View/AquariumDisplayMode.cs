namespace DesktopCompanion.Views
{
    /// <summary>
    /// 아쿠아리움 표시 모드(설정). 같은 탱크 RT를 어느 컨슈머로 보여줄지 결정한다.
    /// Window=개폐 UI 창(현재 구현), Wallpaper=전체화면 배경(후속 컨슈머).
    /// </summary>
    public enum AquariumDisplayMode
    {
        Window,
        Wallpaper,
    }
}
